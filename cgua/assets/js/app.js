document.addEventListener("DOMContentLoaded",()=>{const o=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add("show")),{threshold:.12});document.querySelectorAll(".reveal").forEach(x=>o.observe(x));document.querySelectorAll("[data-count]").forEach(el=>{let done=false,i=new IntersectionObserver(es=>{if(es[0].isIntersecting&&!done){done=true;let n=+el.dataset.count,t=performance.now();function a(x){let p=Math.min((x-t)/1000,1);el.textContent=Math.floor(n*(1-Math.pow(1-p,3))).toLocaleString();p<1&&requestAnimationFrame(a)}requestAnimationFrame(a)}});i.observe(el)})});